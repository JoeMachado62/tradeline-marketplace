{
  "name": "tradeline-marketplace-root",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../admin && npm install",
    "build": "cd admin && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
